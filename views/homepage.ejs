<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Add your head content here -->
</head>
<body>
    <h1>Welcome, <%= isAuthenticated ? user.username : 'Guest' %>!</h1>
    <% if (!isAuthenticated) { %>
        <a href="/login">Login</a>
    <% } %>
</body>
</html>
